# setup.py
from setuptools import setup, find_packages

setup(
    name='CoT_package',
    version='0.1',
    packages=find_packages(),
    install_requires=[],  # 필요한 패키지 있다면 여기에
    author='Ansir',
    description='My test package',
)
